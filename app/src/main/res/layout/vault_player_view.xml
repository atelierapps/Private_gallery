<?xml version="1.0" encoding="utf-8"?>
<!--
  TextureView-backed PlayerView so the video surface can be pinch-zoomed / panned
  with a Compose graphicsLayer transform (a SurfaceView punches through and can't
  be transformed). The controller is off — the viewer draws its own Compose
  controls (play/pause, drag-to-seek scrubber, loop).
-->
<androidx.media3.ui.PlayerView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    app:surface_type="texture_view"
    app:use_controller="false"
    app:resize_mode="fit"
    app:shutter_background_color="@android:color/black" />
