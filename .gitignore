# Android / Gradle
*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build/
/captures/
.externalNativeBuild/
.cxx/
app/build/
/app/release/

# Signing — never commit the vault's release keystore (spec §12)
*.jks
*.keystore

# Java verifier build output
tools/crypto-verify/*.class
